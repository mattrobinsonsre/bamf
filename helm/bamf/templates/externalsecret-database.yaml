{{- if and .Values.core.postgresql.external.enabled .Values.core.postgresql.external.externalSecret.enabled }}
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: {{ include "bamf.fullname" . }}-database-credentials
  labels:
    {{- include "bamf.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: {{ .Values.core.postgresql.external.externalSecret.secretStoreRef.name }}
    kind: {{ .Values.core.postgresql.external.externalSecret.secretStoreRef.kind }}
  target:
    name: {{ include "bamf.fullname" . }}-database-credentials
    creationPolicy: Owner
  data:
    - secretKey: password
      remoteRef:
        key: {{ .Values.core.postgresql.external.externalSecret.remoteRef.key }}
        property: {{ .Values.core.postgresql.external.externalSecret.remoteRef.property }}
{{- end }}
