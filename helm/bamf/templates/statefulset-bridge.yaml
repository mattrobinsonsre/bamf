{{- if .Values.outpost.enabled }}
# Bridge scaling: docs/operations/scaling.md
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "bamf.fullname" . }}-bridge
  labels:
    {{- include "bamf.labels" . | nindent 4 }}
    app.kubernetes.io/component: bridge
spec:
  serviceName: {{ include "bamf.fullname" . }}-bridge-headless
  {{- if not .Values.outpost.bridge.autoscaling.enabled }}
  replicas: {{ .Values.outpost.bridge.replicas }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "bamf.bridge.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "bamf.bridge.selectorLabels" . | nindent 8 }}
        {{- with .Values.outpost.bridge.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
        prometheus.io/path: "/metrics"
        {{- with .Values.outpost.bridge.podAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      terminationGracePeriodSeconds: {{ .Values.outpost.bridge.terminationGracePeriodSeconds }}
      {{- with .Values.outpost.bridge.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.outpost.bridge.priorityClassName }}
      priorityClassName: {{ . | quote }}
      {{- end }}
      {{- with .Values.outpost.bridge.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: bridge
          {{- with .Values.outpost.bridge.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          image: {{ include "bamf.bridge.image" . }}
          imagePullPolicy: {{ .Values.outpost.bridge.image.pullPolicy }}
          ports:
            - name: tunnel
              containerPort: {{ .Values.outpost.bridge.tunnelPort }}
              protocol: TCP
            - name: health
              containerPort: 8080
              protocol: TCP
          env:
            - name: BAMF_API_URL
              value: http://{{ include "bamf.fullname" . }}-api:8000
            - name: BAMF_TUNNEL_ADDR
              value: ":{{ .Values.outpost.bridge.tunnelPort }}"
            - name: BAMF_HEALTH_ADDR
              value: ":8080"
            - name: BAMF_TUNNEL_DOMAIN
              value: {{ include "bamf.outpost.tunnelDomain" . | quote }}
            {{- if .Values.outpost.name }}
            - name: BAMF_OUTPOST_NAME
              value: {{ .Values.outpost.name | quote }}
            {{- end }}
            # Pod name is predictable: {statefulset}-{ordinal}
            # Bridge extracts ordinal from pod name for bridge ID
            - name: BAMF_POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: BAMF_BRIDGE_ID
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
            - name: BAMF_TLS_MIN_VERSION
              value: {{ .Values.tls.internal.minVersion | default "1.3" | quote }}
            # Shutdown timeout = terminationGracePeriodSeconds - 5s buffer before SIGKILL
            - name: BAMF_SHUTDOWN_TIMEOUT
              value: "{{ sub .Values.outpost.bridge.terminationGracePeriodSeconds 5 }}s"
            {{- if .Values.outpost.bridge.autoscaling.targetTunnelsPerPod }}
            - name: BAMF_TARGET_TUNNELS_PER_POD
              value: "{{ .Values.outpost.bridge.autoscaling.targetTunnelsPerPod }}"
            {{- end }}
            {{- if .Values.outpost.bridge.bootstrap.enabled }}
            - name: BAMF_BOOTSTRAP_TOKEN
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.outpost.bridge.bootstrap.tokenSecret | default (printf "%s-bridge-bootstrap" (include "bamf.fullname" .)) }}
                  key: {{ .Values.outpost.bridge.bootstrap.tokenKey | default "token" }}
            - name: BAMF_DATA_DIR
              value: /var/lib/bamf/bridge
            - name: BAMF_TLS_CERT
              value: /var/lib/bamf/bridge/tls.crt
            - name: BAMF_TLS_KEY
              value: /var/lib/bamf/bridge/tls.key
            - name: BAMF_CA_CERT
              value: /var/lib/bamf/bridge/ca.crt
            {{- end }}
            {{- with .Values.outpost.bridge.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.outpost.bridge.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
            {{- if .Values.outpost.bridge.bootstrap.enabled }}
            - name: data
              mountPath: /var/lib/bamf/bridge
            {{- else }}
            - name: tls
              mountPath: /etc/bamf/tls
              readOnly: true
            {{- end }}
            {{- with .Values.outpost.bridge.extraVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          livenessProbe:
            httpGet:
              path: /health
              port: health
              scheme: HTTP
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /ready
              port: health
              scheme: HTTP
            initialDelaySeconds: 5
            periodSeconds: 5
          resources:
            {{- toYaml .Values.outpost.bridge.resources | nindent 12 }}
          {{- if gt (int .Values.outpost.bridge.preStopSleepSeconds) 0 }}
          lifecycle:
            preStop:
              exec:
                command:
                  - /bin/sh
                  - -c
                  - sleep {{ .Values.outpost.bridge.preStopSleepSeconds }}
          {{- end }}
      volumes:
        {{- if .Values.outpost.bridge.bootstrap.enabled }}
        - name: data
          emptyDir: {}
        {{- else }}
        - name: tls
          secret:
            {{- if .Values.tls.existingSecret }}
            secretName: {{ .Values.tls.existingSecret }}
            {{- else }}
            secretName: {{ include "bamf.fullname" . }}-tls
            {{- end }}
        {{- end }}
        {{- with .Values.outpost.bridge.extraVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.outpost.bridge.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.outpost.bridge.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.outpost.bridge.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
{{- end }}
