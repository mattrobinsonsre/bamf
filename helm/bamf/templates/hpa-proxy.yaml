{{- if and .Values.edge.enabled .Values.edge.proxy.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bamf.fullname" . }}-proxy
  labels:
    {{- include "bamf.labels" . | nindent 4 }}
    app.kubernetes.io/component: proxy
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "bamf.fullname" . }}-proxy
  minReplicas: {{ .Values.edge.proxy.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.edge.proxy.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.edge.proxy.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.edge.proxy.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
{{- end }}
