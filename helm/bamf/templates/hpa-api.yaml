{{- if .Values.core.api.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "bamf.fullname" . }}-api
  labels:
    {{- include "bamf.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "bamf.fullname" . }}-api
  minReplicas: {{ .Values.core.api.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.core.api.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.core.api.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.core.api.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.core.api.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        target:
          type: Utilization
          averageUtilization: {{ .Values.core.api.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
